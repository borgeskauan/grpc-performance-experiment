server:
  port: 8080
  http2:
    enabled: true

spring:
  application:
    name: product-service

grpc:
  server:
    port: 9090
    
logging:
  level:
    com.example.product: INFO
    io.grpc: INFO
